<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Trends_Go_Live_Date_Required</fullName>
    <active>true</active>
    <description>can only be in stage go live and date must be filled in.</description>
    <errorConditionFormula>IF(CONTAINS(RecordType.DeveloperName, &quot;Trend&quot;),
AND(ispickval(StageName,&quot;Go Live!&quot;)  || 
     ispickval(StageName,&quot;Bill&quot;))
&amp;&amp; ISBLANK( Go_Live_Date__c    )

, FALSE)

&amp;&amp; $Profile.Name &lt;&gt;&quot;System Administrator&quot;</errorConditionFormula>
    <errorDisplayField>Go_Live_Date__c</errorDisplayField>
    <errorMessage>You must enter a Go Live! date before going to Go Live! stage.</errorMessage>
</ValidationRule>
