<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>This perm set will disable the auto switching of Classic UI to LEX UI that happens automatically for LEX users.

This perm set should be assigned based only on whether the user needs to work in Classic UI but needs LEX solely for Dashboards</description>
    <hasActivationRequired>false</hasActivationRequired>
    <label>Salesforce Classic Login for LEX Users</label>
    <userPermissions>
        <enabled>true</enabled>
        <name>LtngPromoReserved01UserPerm</name>
    </userPermissions>
</PermissionSet>
